## 刘英哲 2300012753

因为最高可并行进程数量被限制为12，所以在N较大时开销主要集中在计算上，进程间通信开销很小。

所以非常简单地将所有数据大致均分为12份，分发给每个线程各自计算，并向root返回进程总和，之后由root计算每个进程需要添加的offset并分发。每个进程各自加上偏移量后将结果传递回root即可。

P.S. 感觉这玩意没啥优化空间，预计会有大量提交的性能集中在一个小区间内，加之调度存在随机性，建议评性能分的时候将性能相近的提交视为同一排名。